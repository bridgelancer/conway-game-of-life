import styled from 'styled-components'

export const KonvaStageCSS = styled.div`
  .ui.primary.button {
    margin: 1rem;

    background-color: inherit;
    color: inherit;
    border: solid 3px;
    border-radius: 1rem;
  }
`
